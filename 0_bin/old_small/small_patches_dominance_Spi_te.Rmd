---
title: "Untitled"
output: html_document
date: "2023-10-03"
---

```{r}
response_variable = "Spi_te_indiv_per_ml_dominance"
```

```{r}
plot.patches.points(ds_patches,
                    patch_type_input,
                    response_variable)
```

<details>
  <summary>Click for boxplots</summary>
```{r}
plot.patches.boxplots(patch_type_input,
                      response_variable)
```
</details>

<details>
  <summary>Click for replicates</summary>
```{r}
plot.patches.replicates(patch_type_input,
                        response_variable)
```
</details>

<details>
  <summary>Click for effect sizes</summary>
```{r}
plot.patches.points.ES(ds_patches_effect_size, 
                       patch_type_input,
                       paste0(response_variable, "_d"))
```
</details>

###### Small isolated vs Small connected to small

```{r}
patch_type_input_model = c("Small isolated",
                           "Small connected to small")
```

```{r child = here("r_files", "analysis_small_n_large_patches.Rmd")}
```

###### Small connected vs Small connected to large

```{r}
patch_type_input_model = c("Small connected to large",
                           "Small connected to small")
```

```{r child = here("r_files", "analysis_small_n_large_patches.Rmd")}
```