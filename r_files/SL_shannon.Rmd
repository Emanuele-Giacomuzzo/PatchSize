---
title: "SL - Shannon"
output: html_document
date: "2022-12-13"
editor_options: 
  chunk_output_type: console
---

```{r}
response_variable = "shannon_d"
```

```{r}
p_connected_shannon_effect_size = plot.patches.points.ES(ds_patches_effect_size, 
                                                       patch_type_input,
                                                       response_variable) %>%
  print()
```

```{r}
#Don't show cook distances because they are not defined for the last time point 
eval_cook_distances = FALSE
```

##### Small

```{r}
patch_type_input_model = c("Small connected to small",
                           "Small connected to large")
```

```{r child = here("r_files", "analysis_ES.Rmd")}
```

##### Large

```{r}
patch_type_input_model = c("Large connected to small",
                           "Large connected to large")
```

```{r child = here("r_files", "analysis_ES.Rmd")}
```