---
title: "Body size distribution: small patches"
author: "Emanuele Giacomuzzo"
date: "2022-09-08"
output: html_document
editor_options: 
  chunk_output_type: console
---

### Small patches {.tabset .tabset-pills}

```{r echo = FALSE}
include_graphics(here("gifs", "transition_day_S_low.gif"))
include_graphics(here("gifs", "transition_day_S_high.gif"))
```

#### Plots

##### Low disturbance {.tabset .tabset-pills}

###### Abundance Untransformed

```{r}
for(time_point_input in 0:7) {
  print(
    ds_classes_averaged %>%
      filter(disturbance == "low") %>%
      filter(eco_metaeco_type == "S (S_S)" | eco_metaeco_type == "S (S_L)") %>%
      filter(day == time_point_input*4) %>%
      ggplot(
        aes(
          x = log_size_class,
          y = log_abundance,
          group = interaction(log_size_class, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      geom_errorbar(
        aes(ymax = log_abundance_upper_ci,
            ymin = log_abundance_lower_ci),
        width = .2,
        position = position_dodge(0.05)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        legend.position = c(.95, .95),
        legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Day = ", (time_point_input*4)),
        x = 'Log size (μm2)',
        y = 'Log abundance + 1 (indiv/μm2)',
        color = ""
      )
  )
}
```

###### Abundance Effect Size

```{r}
for(time_point_input in 0:7) {
  print(
    ds_classes_averaged %>%
      filter(disturbance == "low") %>%
      filter(eco_metaeco_type == "S (S_S)" | eco_metaeco_type == "S (S_L)") %>%
      filter(day == time_point_input*4) %>%
      ggplot(
        aes(
          x = log_size_class,
          y = abundance_hedges_d,
          group = interaction(log_size_class, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line( 
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        legend.position = c(.95, .95),
        legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Day = ", (time_point_input*4)),
        x = 'Log size (μm2)',
        y = 'Log abundance + 1 (indiv/μm2)',
        color = ""
      )
  )
}
```

##### High disturbance {.tabset .tabset-pills}

###### Abundance Untransformed

```{r}
for(time_point_input in 0:7) {
  print(
    ds_classes_averaged %>%
      filter(disturbance == "high") %>%
      filter(eco_metaeco_type == "S (S_S)" | eco_metaeco_type == "S (S_L)") %>%
      filter(day == time_point_input*4) %>%
      ggplot(
        aes(
          x = log_size_class,
          y = log_abundance,
          group = interaction(log_size_class, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      geom_errorbar(
        aes(ymax = log_abundance_upper_ci,
            ymin = log_abundance_lower_ci),
        width = .2,
        position = position_dodge(0.05)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        legend.position = c(.95, .95),
        legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Day = ", (time_point_input*4)),
        x = 'Log size (μm2)',
        y = 'Log abundance + 1 (indiv/μm2)',
        color = ""
      )
  )
}
```

###### Abundance Effect Size

```{r}
for(time_point_input in 0:7) {
  print(
    ds_classes_averaged %>%
      filter(disturbance == "high") %>%
      filter(eco_metaeco_type == "S (S_S)" | eco_metaeco_type == "S (S_L)") %>%
      filter(day == time_point_input*4) %>%
      ggplot(
        aes(
          x = log_size_class,
          y = abundance_hedges_d,
          group = interaction(log_size_class, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        legend.position = c(.95, .95),
        legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Day = ", (time_point_input*4)),
        x = 'Log size (μm2)',
        y = 'Log abundance + 1 (indiv/μm2)',
        color = ""
      )
  )
}
```

```{r}
for (disturbance_input in c("low", "high")){
for(chosen_size_class in 1:nr_of_size_classes) {
  print(
    ds_classes_averaged %>%
      filter(disturbance == disturbance_input) %>%
      filter(eco_metaeco_type == "S (S_S)" | eco_metaeco_type == "S (S_L)") %>%
      filter(size_class_n == chosen_size_class) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .95),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", disturbance_input, ", Size Class = ", (chosen_size_class)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}}
```


Body size class: 1, disturbance = low

#### Model Time Series {.tabset}

Let's start from the full model (no mixed effect: meta-ecosystems have been pulled to create the lnRR):

$$
d = t + P + D + t*P + t*D + P*D
$$

d = Hedge's d of the abundance of a size class (base level is calculated at each disturbance level and time point as the mean bioarea of the small isolated patches)

t = time

P = patch type

D = disturbance

We will exclude in the model the time point 0 and 1. At time point 0 all cultures were the same because that's how we made them. At time point 1 no disturbance event had already taken place.

```{r choose-time-points-body-size-distribution}
first_time_point = 2
last_time_point = 7
```

##### Size Class: 1

```{r choose-size-class-1}
chosen_size_class = 1
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is the null model.

##### Size Class: 2

```{r}
chosen_size_class = 2
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

$$
d = day + patch \: type + day*patch \:type
$$


Let's then do some model diagnostics.

```{r eval = FALSE}
par(mfrow = c(2,3))
plot(best_model, which = 1:5)
```

```{r eval = FALSE}
R2_full = glance(best_model)$r.squared
no_ecometaeco_type = lm(abundance_hedges_d ~
                          day,
                        data = data)

R2_no_ecometaeco_type = glance(no_patch_type)$r.squared
R2_ecometaeco_type = R2_full - R2_no_ecometaeco_type

R2_full = round(R2_full, digits = 2)
R2_no_ecometaeco_type = round(R2_no_ecometaeco_type, digits = 2)
```

The adjusted R squared of the model is `r R2_full` and the adjusted R squared of patch type is `r R2_P` (which includes also its interaction with disturbance).

##### Size Class: 3

```{r}
chosen_size_class = 3
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 4

```{r}
chosen_size_class = 4
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 5

```{r}
chosen_size_class = 5
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 6

```{r}
chosen_size_class = 6
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 7

```{r}
chosen_size_class = 7
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 8

```{r}
chosen_size_class = 8
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 9

```{r}
chosen_size_class = 9
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 10

```{r}
chosen_size_class = 10
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 11

```{r}
chosen_size_class = 11
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is:

##### Size Class: 12

```{r}
chosen_size_class = 12
```

```{r}
for (disturbance_input in c("low", "high")) {
  print(
    ds_classes_averaged %>%
      filter(size_class_n == chosen_size_class) %>%
      filter(eco_metaeco_type == "S (S_S)" |
               eco_metaeco_type == "S (S_L)") %>%
      filter(disturbance == disturbance_input) %>%
      ggplot(
        aes(
          x = day,
          y = abundance_hedges_d,
          group = interaction(day, eco_metaeco_type),
          color = eco_metaeco_type
        )
      ) +
      geom_point(stat = "summary", fun = "mean") +
      geom_line(
        stat = "summary",
        fun = "mean",
        aes(group = eco_metaeco_type)
      ) +
      scale_color_manual(
        values = c(colour_different_size, colour_same_size),
        labels = c("Small connected to large",
                   "Small connected to small")
      ) +
      theme_bw() +
      theme(
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        #legend.position = c(.95, .2),
        #legend.justification = c("right", "top"),
        legend.box.just = "right",
        legend.margin = margin(6, 6, 6, 6)
      ) +
      labs(
        title = paste0("Disturbance = ", (disturbance_input)),
        x = 'Day',
        y = 'Hedge\'s d',
        color = ""
      )
  )
}
```

```{r}
data = ds_classes_averaged %>%
  filter(
    eco_metaeco_type == "S (S_S)" |
      eco_metaeco_type == "S (S_L)",
    time_point >= first_time_point,
    time_point <= last_time_point,
    size_class_n == chosen_size_class
  ) %>%
  ungroup() %>%
  select(eco_metaeco_type, day, disturbance, abundance_hedges_d) 

stepswise_AIC = glmulti(
  y = abundance_hedges_d ~
    day +
    eco_metaeco_type +
    eco_metaeco_type:day +
    eco_metaeco_type:disturbance +
    disturbance +
    disturbance:day,
  data = data,
  fitfunc = lm
)

summary(stepswise_AIC)
```

Therefore, the best model is: