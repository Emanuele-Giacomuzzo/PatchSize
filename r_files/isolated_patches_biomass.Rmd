---
title: "isolated_patches"
author: "Emanuele Giacomuzzo"
date: "2022-08-05"
output: html_document
editor_options: 
  chunk_output_type: console
---

##### Time - Biomass

```{r}
response_variable = "bioarea_µm2_per_ml"
```

```{r}
plot.patches.boxplots(patch_type_input,
                       response_variable)

p_isolated_biomass = plot.patches.points(patch_type_input,
                       response_variable)

p_isolated_biomass
```

*Analysis*

-   Filter data

```{r}
filtered_data = ds_patches %>%
  filter(
    disturbance == disturbance_input,
    metaecosystem == "no",
    time_point >= first_time_point_model,
    time_point <= last_time_point_model
  )
```

-   Full model

```{r}
full_model = lmer(
  bioarea_µm2_per_ml ~
    day +
    patch_size_ml +
    patch_size_ml : day +
    baseline_bioarea_µm2_per_ml + 
    baseline_bioarea_µm2_per_ml : day +
    (day | culture_ID),
  data = filtered_data,
  REML = FALSE,
  control = lmerControl(optimizer = "Nelder_Mead")
)

summary(full_model)
plot(full_model)
qqnorm(resid(full_model))
```

-   Test `patch_size_ml` and `patch_size_ml : day`

```{r}
null_model = lmer(
  bioarea_µm2_per_ml ~
    day +
    baseline_bioarea_µm2_per_ml + 
    baseline_bioarea_µm2_per_ml : day +
    (day | culture_ID),
  data = filtered_data,
  REML = FALSE,
  control = lmerControl(optimizer = "Nelder_Mead")
)

summary(null_model)
plot(null_model)
qqnorm(resid(null_model))
stats = compute.model.stats(full_model,
                            null_model,
                            "mixed_model")

stats
```

-   Test `patch_size_ml`

```{r}
null_model = lmer(
  bioarea_µm2_per_ml ~
    day +
    patch_size_ml : day +
    baseline_bioarea_µm2_per_ml + 
    baseline_bioarea_µm2_per_ml : day +
    (day | culture_ID),
  data = filtered_data,
  REML = FALSE,
  control = lmerControl(optimizer = "Nelder_Mead")
)

summary(null_model)
plot(null_model)
qqnorm(resid(null_model))
stats = compute.model.stats(full_model,
                            null_model,
                            "mixed_model")

stats
```

-   Test `patch_size_ml : day`

```{r}
null_model = lmer(
  bioarea_µm2_per_ml ~
    day +
    patch_size_ml +
    baseline_bioarea_µm2_per_ml + 
    baseline_bioarea_µm2_per_ml : day +
    (day | culture_ID),
  data = filtered_data,
  REML = FALSE,
  control = lmerControl(optimizer = "Nelder_Mead")
)

summary(null_model)
plot(null_model)
qqnorm(resid(null_model))
stats = compute.model.stats(full_model,
                            null_model,
                            "mixed_model")

stats
```

##### Patch size - Biomass {.tabset .tabset-pills}

###### Isolated

```{r}
plot.patches.size.response.boxplots(metaecosystem_input = "no",
                                  trophic_type_input = "NA",
                                  flow_input = "NA",
                                  response_variable = response_variable)
```

###### Connected

```{r}
plot.patches.size.response.boxplots(metaecosystem_input = "yes",
                                  trophic_type_input = "NA",
                                  flow_input = "NA",
                                  response_variable = response_variable)
```
